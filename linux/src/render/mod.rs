// WebKitGTK rendering layer: custom `mdreader://` scheme (serves bundled resources from the
// GResource) + the `mdreaderNative` JS bridge (mirrors macOS MarkdownWebView.bridgeScript).

pub mod webview;
